import styled from "styled-components";

export const SpotifyWrap = styled.span`
  display: flex;
  align-items: center;
  color: ${({ theme }) => theme.color};
  line-height: 1.5;
  font-size: 0.9rem;
  margin-left: 10px;
`;
